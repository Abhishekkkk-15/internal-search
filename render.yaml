services:
  - type: web
    name: nexus-assistant-api
    env: node
    plan: free
    buildCommand: pnpm install && pnpm --filter api... build
    startCommand: node apps/api/dist/index.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
