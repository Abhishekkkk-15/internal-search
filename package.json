{
  "name": "nexus-assistant-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.29.2",
  "scripts": {
    "dev": "turbo run dev",
    "build": "pnpm --recursive build",
    "build:ai": "pnpm --filter @nexus/ai run build",
    "lint": "pnpm --recursive lint",
    "lint:ai": "pnpm --filter @nexus/ai run lint"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.10",
    "turbo": "^2.9.14",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "jsonwebtoken": "^9.0.3"
  }
}
